package helpers;

//import data.Summer;
import GUI.Driver;


//helpers package typically includes classes with static methods
public class StateManager {
	
/*	//an enum is like making our own variable
	public static enum GameState {
		SPRING, SUMMER, FALL, WINTER, YEAREND
	}

	//look at what part of the program and edit appropriate things
	public static GameState gameState;

	public static Spring spring;
	public static Summer summer;
	public static Fall fall;
	public static Winter winter;
	public static YearEnd yearEnd;
	
	public static String currentState;
	
	public static void update() {
		//iterate through one round
		for(int i = 0; i < 5; i++) {
			switch(i) {
			case 0 :
				//what will happen when gamestate is equal to SPRING
				currentState = "spring";
				if(Driver.springDone) {
					
				}
				break;
			case 1 :
				//what will happen when gamesate is equal to SUMMER
				currentState = "summer";
				summer.update();
				break;
			case 2 :
				currentState = "fall";
				fall.update();
				break;
			case 3 :
				currentState = "winter";
				winter.update();
				break;
			case 4 :
				currentState = "yearEnd";
				yearEnd.update();
				break;
			}
		}
	}*/
}

